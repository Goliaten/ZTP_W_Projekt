from typing import Any, Generator, List


def windower(
    signal: List[Any], length: int, overlap: int
) -> Generator[List[Any], None, List[Any]]:
    # function which should return a generator, which returns a windows from the signal

    # should apply window function such as hamming, hanning
    # these windows should have sme length as window

    # TODO implement
    raise NotImplementedError()
