[project]
name = "ztp-w-projekt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scikit-learn",
]

[dependency-groups]
dev = [
    "mypy",
    "ruff",
]
